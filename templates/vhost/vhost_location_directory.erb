
  location <%= name %> {
    <% if defined? alias_dir %>root <%= root_dir %>;
    <% if defined? alias_dir %>alias <%= alias_dir %>; 
    index <% index_files.each do |i| %> <%= i %> <% end %>; 
  }
